<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="2dp">

        <TextView
            android:text="@string/your_rating"
            android:textSize="24sp"
            android:layout_marginTop="10dp"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:layout_gravity="center"/>

        <RatingBar
            android:layout_marginTop="10dp" android:layout_marginBottom="10dp"
            android:layout_width="wrap_content" android:layout_height="wrap_content"
            android:clickable="true"
            android:id="@+id/dialogRatingBar"
            android:layout_gravity="center"
            android:numStars="5" android:stepSize="0.5" android:rating="1.0"
            android:isIndicator="false"/>

        <Button android:id="@+id/dialogRatingSubmitButton"
            android:layout_height="wrap_content"
            android:text="@string/submit"  android:focusable="false"
            android:layout_margin="5dip" android:textColor="#FFF8C6"
            android:textSize="20sp" android:layout_width="fill_parent" />

</LinearLayout>