The methods are not consistent with the logs since when the device is rotated the entire activity is recreated. To make it consistent with the logs there are multiple ways one can do it, but the most common method that is used is to save whatever is there in the UI in a bundle in the ‘onSaveInstanceState’ method. And then refill the UI elements from the bundle in the ‘onRestoreInstanceState’ method.